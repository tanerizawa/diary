# Lokasi: ./requirements.txt
# Deskripsi: Daftar semua dependensi Python yang dibutuhkan oleh proyek.
# Instal dengan 'pip install -r requirements.txt'.

fastapi
uvicorn[standard]
sqlalchemy
pydantic[email]
passlib[bcrypt]
python-jose[cryptography]
python-multipart

# Tambahkan driver database jika Anda tidak menggunakan SQLite
# Contoh untuk PostgreSQL:
# psycopg2-binary